name: proviso
description: A Flutter package for widgets conditional rendering and wrapping.
version: 0.1.0
author: Alexey Perov
homepage: https://github.com/AlexeyPerov/proviso

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

